#!/usr/bin/env python

from distutils.core import setup

setup(name='text',
      version='0.1',
      description='Text processing toolkit',
      packages=['text'],
      package_dir={'': 'lib'},
     )
